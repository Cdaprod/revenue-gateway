/**
 * SDK types re-exported for convenience.
 *
 * Usage:
 *   import type { PlanId, Entitlements } from '@cdaprod/revenue-gateway-sdk';
 */
export type { PlanId, Entitlements } from '@cdaprod/revenue-config';
